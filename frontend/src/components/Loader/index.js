import React from "react";
import {
    CircularProgress
  } from "@mui/material";

const Loader = () => {
    return(
        <CircularProgress className="loaderCircle" />
    );
};

export default Loader;